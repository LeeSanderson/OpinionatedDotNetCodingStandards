## Parent PRD

`issues/prd-xunit-v3-parallel-threads.md`

## What to build

Add a test (or untestable declaration) for rule **MA0205** — *Use exclusive or operator* — from the
`Meziantou.Analyzer` analyzer.

## Acceptance criteria

- [ ] Either a `[Fact]` with `[RuleDoc("MA0205", ...)]` exists in `tests/.../MeziantouAnalyzers/MeziantouAnalyzers3Should.cs`, or a
      class-level `[RuleDoc("MA0205", ..., Untestable = "...")]` exists in `UntestableRules.cs`
- [ ] `RuleDocCoverageShould` passes (no duplicate or missing `[RuleDoc]` entries)
- [ ] If testable: `dotnet test --no-build --filter "FullyQualifiedName~UseExclusiveOrOperator"` passes

## How to implement the test

Add a `[Fact]` to `tests/Opinionated.DotNet.CodingStandards.Tests/MeziantouAnalyzers/MeziantouAnalyzers3Should.cs`:

```csharp
[Fact]
[RuleDoc("MA0205", "Use exclusive or operator",
    HelpLink = "https://github.com/meziantou/Meziantou.Analyzer/blob/main/docs/Rules/MA0205.md")]
public async Task UseExclusiveOrOperator()
{
    using var project = await CreateProjectBuilderAsync();
    await project.AddFileAsync("Program.cs", """
        namespace test;
        // TODO: add code that triggers MA0205 (e.g. (a | b) & ~(a & b) instead of a ^ b)
        public static class Program { public static int Main() => 0; }
        """);
    var buildOutput = await project.BuildAndGetOutputAsync();
    buildOutput.HasError("MA0205").ShouldBeTrue();
}
```

## Confounder playbook (exhaust before marking untestable)

Work through these in order:

1. **Trigger candidate**: use `(a | b) & ~(a & b)` or equivalent XOR-via-OR/AND pattern instead of `a ^ b`.
2. **Use stub types** if the rule guards on an external type.
3. **Check `EnforceOnBuild`** in the Meziantou source if the rule stays silent.
4. **Check the reporting analyzer's assembly path** — `src/Analyzers/` = builds; `src/Features/` = IDE-only.

Only move the rule to `UntestableRules.cs` after exhausting all of the above.

## Blocked by

None — can start immediately.

## User stories addressed

- User story 2 (add test coverage for each newly-discovered rule)
